Wofür steht RMI in Java und was zeichnet es aus? Welche charakteristischen Eigenschaften besitzt es im Hinblick auf Client-Server-Kommunikation?

Wie lässt sich ein paralleler TCP Server realisieren? Gehen Sie kurz auf die Eigenschaften der von Ihnen aufgeführten Lösungen ein.
(Statische Parallelität, Dynamische Parallelität, Mischformen)

Was ist datenstromorientierte und datagram-orientierte Communication oim Zusammenhang mit Sockets? Welche Zusammenhänge zur parallelen Programmierung kennen Sie? (Message Queue, Pipes

Realisieren sie ein RMI-Programm, bei dem die Anfrage eines Clients und somit der Auftrag an ein RI-Objekt in einen Thread ausgelagert wird.

Schreiben Sie ein RMI-Programm, bei dem mittels Call-bei-Reference Wertübergabe und -rückgabe realisiert ist.

Schreiben Sie ein Beispiel für das Entfernen von Elementen mit Hilfe eines Iterators.

Zu welcher Schicht ist das HTTP-Protokoll zuzuordnen, und was zeichnet es aus?

Erläutern sie das Sperren von Datensätzen mittels Cookies, und wieso es die Nutzung von synchronized nicht überflüssig macht.

Schreiben Sie eine Anwendung, bei der ein modales Fenster auf die Eingabe eines Users wartet, und diese Eingabe dann an die öffnende Komponente weiterleitet.

Erklären Sie mit eigenen Worten, was eine Fluent-Api ist, und implementieren Sie ein Beispiel anhand der JavaFX-Properties.

Beschreiben Sie den Unterschied zwischen verzahnt und parallel arbeitenden TCP-Sockets.

Wie kanne ine lokale Anwendung in eine verteilte Anwendung transformiert werden?